The variable construction in 01b_stock_variables.py applies winsorization within each monthly cross-section via groupby('date').transform(winsorize), which uses only the current month's distribution to set the 1st/99th percentile bounds. This is method (b) — look-ahead-free. No re-run is required; the baseline FM t(ΔG) = -3.981 is already computed under within-month winsorization.